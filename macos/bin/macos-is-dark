#!/usr/bin/env zsh
defaults read -g AppleInterfaceStyle &>/dev/null
if [ $? -eq 0 ]; then
  return 0 # The value is "Dark", so dark mode is active
else
  return 1 # The value is not "Dark" (unset or "Light"), so light mode is active
fi
