#!/usr/bin/env zsh

fzbrews() {
  local pkg
  pkg=$(
    brew formulae |
      fzf \
        --query "$1" \
        --preview="brew info {}" \
        --bind="ctrl-x:execute-silent(brew home {})"
  )

  if [[ -n "$pkg" ]]; then
    brew info "$pkg"
  fi
}
