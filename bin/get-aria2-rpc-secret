#!/usr/bin/env zsh
SECRET_FILE="/usr/local/share/aria2/secret"

autoload -Uz colors && colors

get-aria2-rpc-secret() {
  if [[ ! -f "$SECRET_FILE" ]]; then
    echo "$fg[red] ${reset_color} aria2 secret not found at: $SECRET_FILE"
    return 1
  fi
  echo $(cat --plain "$SECRET_FILE")
}
