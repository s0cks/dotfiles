#!/usr/bin/env zsh
cap() {
  local tool="${SCREENCAPTURE_TOOL:-screencapture}"
  local out_dir="${SCREENCAPTURE_OUT_DIR:-"$HOME/Screen Captures"}"
  local timestamp="$(date ${SCREENCAPTURE_TIMESTAMP_PATTERN:-'+%Y%m%d_%H%M%S'})"

  $tool "$out_dir/$timestamp.png"
}
